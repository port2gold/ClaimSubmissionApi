using AutoMapper;
using ClaimSubmissionApi.Data.Dtos;
using ClaimSubmissionApi.Model;

namespace ClaimSubmissionApi.Data
{
    public static class MapperConfig
    {
        //public static void Map()
        //{
        //    var config = new MapperConfiguration(cfg => cfg.CreateMap<MakeClaimDto, Claim>());

        //    var mapper = new Mapper(config);
        //    MakeClaimDto dto = mapper.Map<MakeClaimDto>(Claim);
        //}
    }
}
